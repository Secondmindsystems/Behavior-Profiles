# Scope Control Limitations

Scope Control is an instruction-layer profile.

It does not block file access, edits, commands, commits, or external actions. It cannot guarantee that an agent will interpret or follow the profile consistently.

The same profile may produce different behavior across models, versions, tools, instruction precedence, and task phrasing.

A test result describes only the declared episode. Human review remains required.
